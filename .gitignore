.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.audit/

node_modules/
dist/
build/

.env
.env.*
!.env.example
/envs.txt
*.age
/secrets/

.dvc/cache/
artifacts/
!docs/evals/**/artifacts/
!docs/evals/**/artifacts/**
data/raw/
/models/

evidence.local.yaml
*.local.yaml

.idea/
.vscode/
.DS_Store
Thumbs.db

# Host credential handoffs. These carry SSH passwords, keys, and endpoints and must never
# be committed; they exist only on the operator workstation.
/newinfo-*/
/oldinfo-*/

# Crash dumps left by Windows/Cygwin tooling.
*.stackdump

# Operator-local execution conventions (internal host addresses, fallback endpoints).
# AGENTS.md states where lessons must be written; it no longer carries them.
# The durable lessons live in docs/governance/GOVERNANCE_V2.md and docs/engineering/.
/CLAUDE.md
