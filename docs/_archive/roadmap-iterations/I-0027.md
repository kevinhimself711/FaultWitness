# I-0027 — Third Replacement Unified Candidate Orchestration

## 状态

Terminal `failed` on candidate `a5c282cbd587202cb24cd6b1fcc1e57d9785acd6`。除 Gate Eval 外正常预计 **0h30**；Gate Eval 仍为
**2h33**，均不是 kill timeout。

## 目标

在 I-0026 关闭后冻结新候选，通过 EVAL-G02-012 顺序执行十四个既有 phase，完成
reconciliation 与 close-readiness，不在 orchestration 现场新增实现。

## 可证伪假设

新候选能够只编排已冻结 runner，通过全部 Gate 标准且无 waiver、fallback、blocked、
`infra_failed` 或 open evidence。

## 硬边界

确定性 implementation/policy/metric/quality/performance/reconciliation/close-readiness 失败会
使 I-0027 永久 `failed`，并继续创建更高编号的前向 corrective/replacement Iteration。仅已
分类的瞬时基础设施或传输故障可在同一 phase/trial 续跑。

## 结果

候选绑定准备所需的 sanitized environment probe 通过 I-0026 transport 上传 shell script，
但 Windows `subprocess.run(text=True)` 在 SSH stdin 前把 LF 改写为 CRLF，远端 `/bin/sh`
返回 exit 2。真实本地 child-process byte probe 复现了 `0a` 变为 `0d0a`。

这是确定性 runner transport 缺陷，不是瞬时基础设施故障。candidate binding 与 phase
journal 均未创建，十四个 Gate phase、所有 matrix/scenario/external-service/model execution
均为 0。EVAL-G02-012 以 `open_evidence: []` 固化负证据；I-0028 前向修复 byte-exact
transport，I-0029 承担替代编排。
