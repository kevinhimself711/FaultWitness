# A-G02-001 — Unified Candidate Gate Attempt

## 状态

Terminal `failed` on candidate `91c2ce4dc27d6e7e661b9fa6315605ba44124a97`。它是 Gate
attempt，不是 Master Plan Iteration，也不计入 corrective 工程成本。
Gate execution 与 governance synchronization 分账。

## 目标

C-G02-001 关闭后冻结新候选，通过 EVAL-G02-024 顺序编排十四个既有 phase，完成
reconciliation 与 close-readiness。

## 硬边界

本 attempt 不得改动 `src/`、`deploy/`、`tests/`、`config/` 或 `schemas/`，不得新增 runner、
fixture、framework、诊断工具或阈值。确定性失败保留为该 attempt 的终态证据，并前向创建新的
`C-G02-nnn`；瞬时 `infra_failed` 只从失败 trial 续跑。

## 结果

四个 preflight 与真实 lab deployment 通过；60-cell 矩阵前 57 个 cell 通过，第 58 个
`obs:langsmith|canonical-owner` 返回 expected allow 与 actual deny，phase 确定性
`metric_fail`。剩余两 cell 与全部 downstream phase 未启动，破坏性动作和模型调用均为 0。
负面证据完整，`open_evidence: []`；修复只通过 C-G02-002 前向进行。
