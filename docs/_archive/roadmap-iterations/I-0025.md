# I-0025 — Second Replacement Unified Candidate Orchestration

## 状态

Terminal `failed` on frozen candidate `9e68e2622d18ffcbd63549a4fd2b9dedee37f74e`。
除 Gate Eval 外正常预计 **0h30**，Gate Eval 仍为 **2h33**；两者均不是 kill timeout。

## 目标

在 I-0024 关闭后冻结一个新候选，通过 EVAL-G02-010 依序执行十四个既有 phase，完成
reconciliation 与 close-readiness，不在 orchestration 现场新增实现。

## 可证伪假设

新候选能够只编排已冻结 runner，通过全部 Gate 标准且无 waiver、fallback、blocked、
`infra_failed` 或 open evidence。

## 硬边界

确定性 implementation/policy/metric/quality/performance/reconciliation/close-readiness 失败会
使 I-0025 永久 `failed`，并创建更高编号的前向 corrective/replacement Iteration。仅已分类
的瞬时基础设施或传输故障可在同一 phase/trial 续跑。

I-0025 activation freezes its commit as the business candidate. Candidate-binding and phase
artifacts are produced while the checkout remains exactly at that commit, then enter Git only as
evidence-only descendants after execution.

## 结果

四个 fail-fast preflight 与 `lab-deploy-and-bind` 通过。`isolation-access-matrix` 在 provisioning
及首个 cell 前因 Windows `CreateProcess` `WinError 206` 终止：完整 probe 和 request 被嵌入
privileged SSH child-process command line，超过平台长度限制。这是确定性 runner transport
缺陷，不是瞬时基础设施故障；同候选未重试。

EVAL-G02-010 固化 `blocked` 负证据，所有 matrix cell、破坏性 scenario、external-service
probe 与 model call 均为 0，`open_evidence: []`。I-0026 前向修复 bounded transport，I-0027
负责新候选替代编排；I-0024 与 I-0025 永不重开。
