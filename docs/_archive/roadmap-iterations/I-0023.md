# I-0023 — Replacement Unified Candidate Orchestration

## Status

Terminal `failed` on frozen candidate `7d6648850108129c81fe98260aff8342683b5622`.
Expected orchestration time excluding Gate Eval: **0h30**. Gate
Eval remains **2h33**. These are observability estimates, never kill timeouts.

## Objective

Run the same fourteen frozen phases on one post-corrective candidate through EVAL-G02-008. Preserve
EVAL-G02-005 as an immutable failed attempt and close with `open_evidence: []`.

## Falsifiable hypothesis

The corrected candidate passes all fourteen phases without adding behavior, runners, fixtures,
test frameworks, thresholds, or samples during orchestration.

## Hard boundary

Any implementation, policy, zero-tolerance, cleanup, metric, quality, or performance failure makes
I-0023 terminally failed and creates another forward corrective plus replacement orchestration.
Transient infrastructure failures resume only the affected phase or trial.

Planned commit: `test(g02): run replacement fault lab and baseline candidate`.

## Result

The four fail-fast preflights and `lab-deploy-and-bind` passed. Runner-readiness then proved that
the three I-0018 Gate handlers only validated operator-precomputed JSON: no candidate-bound runner
provisioned identities/storage or collected the 60 access cells, six trace stages, and 22 canary
surfaces. No complete matrix cell, destructive scenario, or model call ran.

EVAL-G02-008 records the deterministic `blocked` result with `execution_count: 0` and
`open_evidence: []`; evidence commit `be28b5a848367dcf0e7955e171b75790c2c90866` preserves the
candidate-bound artifacts. I-0023 is immutable and will never be reactivated. I-0024 owns the
forward runner correction and I-0025 is the replacement orchestration.
