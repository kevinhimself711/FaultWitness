# A-G02-002 — Post-LangSmith Unified Candidate Gate Attempt

## 状态

Terminal `failed` on candidate `c51072931984865c900a5cac43ec907be82d0bb1`。它是与 C-G02-002 分离的 Gate attempt，不是 Master Plan Iteration，也不计入
corrective 工程成本。

## 目标

在 C-G02-002 关闭后冻结新候选，执行新候选所需的 fail-fast binding/dependency phases、
受影响的 60-cell access phase 及其 downstream Gate phases，完成 reconciliation 与
close-readiness。A-G02-001 的 57 个 passing cell 只作为不可变历史，不被改写或冒充新候选证据。

## 硬边界

本 attempt 不得改动 `src/`、`deploy/`、`tests/`、`config/` 或 `schemas/`，不得新增 runner、
fixture、framework、诊断工具或阈值。确定性失败保留为终态证据并前向创建新的
`C-G02-nnn`；瞬时 `infra_failed` 只从失败 trial 续跑。

## Result

Four preflights and the real lab deployment passed. The access matrix persisted 58 trials: the
first 57 passed, including the corrected LangSmith canonical-owner credential cell; trial 58,
`obs:langsmith|baseline-agent`, had `expected_allow: true` but made no progress before the runner
recorded `infra_failed`. No downstream, destructive, Bailian, token, or cost phase ran.
EVAL-G02-026 is complete negative evidence with `open_evidence: []`; repair proceeds only through
the forward C-G02-003 work item.
