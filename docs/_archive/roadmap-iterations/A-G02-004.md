# A-G02-004 — Post-Trace-Binding Unified Candidate Gate Attempt

## Status

Terminal `failed` on candidate `723c056defa11355b3c3ad48648c5d9deb4961b8`. This Gate attempt
remains separate from completed C-G02-004 and every earlier terminal attempt.

## Objective

After C-G02-004 closes, freeze a new candidate and orchestrate the existing fourteen Gate phases in
their frozen order under EVAL-G02-030. Prior attempt evidence remains immutable history.

## Hard boundary

The attempt may not modify `src/`, `deploy/`, `tests/`, `config/`, or `schemas/`, and may not add a
runner, fixture, framework, diagnostic tool, threshold, or operator pass route. Deterministic
failure terminates the attempt and creates a forward corrective; classified transient failures
resume only the affected atomic unit.

## Result

Four preflights, the candidate-bound lab, all 60 access cells, all six trace stages, and all 22
canary surfaces passed exactly once. The destructive scenario phase then passed
`SEED-G02-0001` and deterministically failed `SEED-G02-0002` because the
`paymentUnreachable` oracle did not reach `FAULT_ACTIVE`. Exact flag restoration and recovery did
not fail, and the destructive phase was not retried on this candidate.

The terminal trial did not persist its last raw live-observer sample, so the suspected
caller-versus-callee trace attribution seam is not asserted as proven. EVAL-G02-030 retains the
complete negative boundary with `open_evidence: []`; attribution and any minimum fix proceed only
through C-G02-005. No baseline, Bailian call, model token, or model cost work ran.
