# A-G02-007 — Email-Memory Unified Candidate Gate Attempt

## Status

Terminally failed on candidate `0ef51cdb5987aa8b1c5cf17e712fe513f1d33b74`. C-G02-007 remains
complete; no completed work item is reopened.

## Objective

Orchestrate the frozen fourteen-phase G02 Gate Eval under EVAL-G02-036. Explicitly inherit only
semantically unchanged evidence: the 60-cell isolation, six-stage trace, 22-surface canary matrices,
and passed scenario seeds 1–7. Execute scenario seeds 8–32 and every affected downstream baseline,
reconciliation, and close-readiness phase.

## Hard boundary

This attempt may not modify product behavior, fixtures, test semantics, thresholds, or samples.
A deterministic failure terminates the attempt; a classified transient failure resumes only the
affected trial. Candidate-bound deployment runs from a clean detached candidate checkout so
governance HEAD changes cannot rebind the runtime.

## Result

Candidate/static preflight and candidate-bound lab deployment passed with 24/24 Ready Deployments.
The unchanged 60-cell isolation, six-stage trace, 22-surface canary, and scenario seeds 1–7 were
inherited without execution. Seeds 8–12 executed once and passed. SEED-G02-0013 then failed because
the Kafka fault oracle did not reach `FAULT_ACTIVE` inside the frozen 90-second deadline.

Post-failure read-only evidence showed the feature flag and sleep signal were present, while
Prometheus did not expose a positive consumer-lag value until after the deadline. The runner relied
on incidental load-generator checkout timing instead of producing one deterministic Kafka workload.
This is a runner-control defect, not a threshold or infrastructure failure. Later scenarios and all
baseline phases did not run; model calls, tokens, cost, fallback, waivers, and open evidence are zero.
