# I-0035 — Seventh Replacement Unified Candidate Orchestration

## 状态

Terminal `failed`。除 Gate Eval 外正常预计 **0h30**；估算不是 kill timeout。

## 目标

在 I-0034 关闭后冻结新候选，通过 EVAL-G02-020 顺序执行十四个既有 phase，完成
reconciliation 与 close-readiness，不在 orchestration 现场新增实现。

## 可证伪假设

新候选只编排已冻结 runner，即可通过全部 Gate 标准且无 waiver、fallback、blocked、
`infra_failed` 或 open evidence。

## 硬边界

确定性 implementation/policy/metric/quality/performance/reconciliation/close-readiness 失败使
I-0035 永久 `failed`，并只能创建更高编号的前向 corrective/replacement Iteration。仅已
分类的瞬时基础设施或传输故障可在同一 phase/trial 续跑。

## 结果

EVAL-G02-020 通过四个 preflight 与 `lab-deploy-and-bind`，24 个 Deployment ready。访问
矩阵前 24 个 database cells 通过；第 25 个 `s3:g02/scenarios/|canonical-owner` 因 runner
使用隐含 `ListBucket` 的 `mc stat` 而确定性 `metric_fail`。policy、用户与 sentinel 均正确，
后续 phase 和模型调用均为 0。负面证据完整，`open_evidence: []`；修复仅通过 I-0036 前向进行。
