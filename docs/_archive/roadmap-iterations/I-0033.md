# I-0033 — Sixth Replacement Unified Candidate Orchestration

## 状态

Terminal `failed` on candidate `1bc74b9d976cd5721eb9f57f4587a9fb83f35dc8`。除 Gate Eval
外正常预计 **0h30**；Gate Eval 仍为 **2h33**，均不是 kill timeout。

## 目标

在 I-0032 关闭后冻结新候选，通过 EVAL-G02-018 顺序执行十四个既有 phase，完成
reconciliation 与 close-readiness，不在 orchestration 现场新增实现。

## 可证伪假设

新候选只编排已冻结 runner，即可通过全部 Gate 标准且无 waiver、fallback、blocked、
`infra_failed` 或 open evidence。

## 硬边界

确定性 implementation/policy/metric/quality/performance/reconciliation/close-readiness 失败会使
I-0033 永久 `failed`，并继续创建更高编号的前向 corrective/replacement Iteration。仅已
分类的瞬时基础设施或传输故障可在同一 phase/trial 续跑。

## 结果

四个 fail-fast preflight 全部通过。`lab-deploy-and-bind` 已拉取、传输并 import 两个 probe
archive，但 K3s containerd 将 exact `minio/mc` digest 登记为 `index.docker.io` source；冻结
runner 只查 `docker.io` source，因而在 tag/apply 前 exit 1。

这是确定性 imported-reference alias 解析缺陷，不是传输抖动。new-candidate deployment、
60 access cells、trace/canary/scenario、external service 与 model execution 均为 0。
EVAL-G02-018 以 `open_evidence: []` 固化负证据；I-0034 前向修复 exact-digest alias
resolution，I-0035 承担替代编排。I-0032 与 I-0033 永不重开。
