# Legacy Iteration lifecycle

The per-Iteration plan/activate/evidence/close lifecycle ended with G02. It is not an active
workflow, template, CI input, or recovery path. Its complete contract and historical records are
preserved at `gate/G02-v1`.

Current work uses one Gate Plan, the journaled direct-debug loop in `AGENTS.md`, and one release
manifest at Gate closure. A local failure does not reopen completed work or create a corrective or
Gate-attempt lifecycle record.
