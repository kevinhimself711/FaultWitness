# C-G02-005 — Payment-Unreachable Caller-Trace Observation Corrective

## Status

Active. This corrective owns only the `paymentUnreachable` caller/callee observation seam exposed
by EVAL-G02-030; it does not reopen A-G02-004 or I-0017.

## Falsifiable first action

Use the exact read-only EVAL-G02-031 command to compare sanitized `checkout` and `payment` error
spans in the frozen failed interval. The hypothesis is confirmed only if checkout contains the
payment connection error while the payment-service query cannot supply the required correlated
span. If that contrast is absent, stop and reattribute before changing the observer.

## Minimal scope

If confirmed, change only the live trace source for `paymentUnreachable`; keep `paymentFailure` and
all other adapters unchanged. Reuse `tests/g02/test_g02_lab.py`, then execute only
`SEED-G02-0002` once through the existing scenario runner. Exact flag restoration remains
mandatory and the sanitized artifact must preserve both fault observations.

No new harness, full Gate phase, access/trace/canary matrix, baseline, Bailian call, model call,
threshold, permission, N, oracle, Ground Truth, locked-test, quality/performance, token/cost, or
failure-semantic change is authorized. Closure requires `open_evidence: []`.
