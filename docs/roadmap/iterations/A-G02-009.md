# A-G02-009 — Infrastructure-Classification Unified Candidate Gate Attempt

## Status

Active on candidate `17be4f2d29448bc4db49b71dafe005d7877cd99c`. C-G02-009 is complete;
no completed work item is reopened.

## Objective

Orchestrate the frozen fourteen-phase G02 Gate Eval under EVAL-G02-040. Explicitly inherit only
semantically unchanged evidence: manifest debt, G01 debt, the 60-cell isolation, six-stage trace,
22-surface canary matrices, and passed scenario seeds 1–12. Execute scenario seeds 13–32 and every
affected downstream baseline, reconciliation, and close-readiness phase.

## Hard boundary

This attempt may not modify product behavior, fixtures, test semantics, thresholds, or samples.
A deterministic failure terminates the attempt; a no-observation transport/infrastructure failure
is persisted atomically and resumes only the affected trial. Candidate-bound execution uses the
frozen candidate subjects so governance HEAD changes cannot rebind the runtime.
