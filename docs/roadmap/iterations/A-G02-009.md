# A-G02-009 — Infrastructure-Classification Unified Candidate Gate Attempt

## Status

Terminally failed on candidate `17be4f2d29448bc4db49b71dafe005d7877cd99c`. C-G02-009 remains
complete; no completed work item is reopened.

## Objective

Orchestrate the frozen fourteen-phase G02 Gate Eval under EVAL-G02-040. Explicitly inherit only
semantically unchanged evidence: manifest debt, G01 debt, the 60-cell isolation, six-stage trace,
22-surface canary matrices, and passed scenario seeds 1–12. Execute scenario seeds 13–32 and every
affected downstream baseline, reconciliation, and close-readiness phase.

## Hard boundary

This attempt may not modify product behavior, fixtures, test semantics, thresholds, or samples.
A deterministic failure terminates the attempt; a no-observation transport/infrastructure failure
is persisted atomically and resumes only the affected trial. Candidate-bound execution uses the
frozen candidate subjects so governance HEAD changes cannot rebind the runtime.

## Result

Candidate/static preflight and candidate-bound deployment passed with 24/24 Ready Deployments.
Manifest debt, G01 debt, the unchanged 60/6/22 matrices, and scenario seeds 1–12 were inherited
without execution. SEED-G02-0013 then received an explicit HTTP 504 from the Kafka checkout
stimulus, so the frozen runner correctly recorded a blocking `metric_fail`.

Read-only attribution found fleet-wide POST 504 traces and `accounting` OOM exit 137 at
`2026-07-25T22:21:37Z`, with restart count 62, exactly alongside the failed trial. The dedicated
node had no MemoryPressure, the flag was restored to `off`, and the upstream load generator remained
at its default ten users. This is uncontrolled ambient lab load, not the C-G02-009 no-observation
classification branch. Later scenarios and all baseline/model phases did not run.
