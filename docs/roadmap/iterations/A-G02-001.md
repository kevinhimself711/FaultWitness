# A-G02-001 — Unified Candidate Gate Attempt

## 状态

Planned。它是 Gate attempt，不是 Master Plan Iteration，也不计入 corrective 工程成本。
Gate execution 与 governance synchronization 分账。

## 目标

C-G02-001 关闭后冻结新候选，通过 EVAL-G02-024 顺序编排十四个既有 phase，完成
reconciliation 与 close-readiness。

## 硬边界

本 attempt 不得改动 `src/`、`deploy/`、`tests/`、`config/` 或 `schemas/`，不得新增 runner、
fixture、framework、诊断工具或阈值。确定性失败保留为该 attempt 的终态证据，并前向创建新的
`C-G02-nnn`；瞬时 `infra_failed` 只从失败 trial 续跑。
