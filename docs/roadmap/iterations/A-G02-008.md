# A-G02-008 — Kafka-Workload Unified Candidate Gate Attempt

## Status

Terminally failed on candidate `9adf0ba62f618b15bb41bbb56cec5fdc6229cc8a`. C-G02-008 remains
complete; no completed work item is reopened.

## Objective

Orchestrate the frozen fourteen-phase G02 Gate Eval under EVAL-G02-038. Explicitly inherit only
semantically unchanged evidence: the 60-cell isolation, six-stage trace, 22-surface canary matrices,
and passed scenario seeds 1–12. Execute scenario seeds 13–32 and every affected downstream baseline,
reconciliation, and close-readiness phase.

## Hard boundary

This attempt may not modify product behavior, fixtures, test semantics, thresholds, or samples.
A deterministic failure terminates the attempt; a classified transient failure resumes only the
affected trial. Candidate-bound deployment runs from a clean detached candidate checkout so
governance HEAD changes cannot rebind the runtime.

## Result

Candidate/static preflight and candidate-bound lab deployment passed with 24/24 Ready Deployments.
The unchanged 60-cell isolation, six-stage trace, 22-surface canary, and scenario seeds 1–12 were
inherited without execution. SEED-G02-0013 sent and completed its single synthetic checkout and the
exact Kafka fault log appeared, but a later remote source-collection command exited 1 without
producing an observation. The current runner caught that `GovernanceError` and wrote `metric_fail`.

Post-failure read-only collection succeeded after the affected `accounting` pod recovered from a
concurrent OOM restart. Because no metric observation existed to compare with the oracle, the failure
was an infrastructure/collection condition, not a threshold result. Later scenarios and all baseline
phases did not run; model calls, tokens, cost, fallback, waivers, and open evidence are zero.
