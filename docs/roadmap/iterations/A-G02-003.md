# A-G02-003 — Post-Egress Unified Candidate Gate Attempt

## Status

Terminal `failed` on candidate `fbd03c464904b0969fdff9a8c89170435b80ef81`. This Gate attempt
is separate from C-G02-003 and from every earlier terminal attempt.

## Objective

After C-G02-003 closes, freeze a new candidate and execute the existing fourteen Gate phases in
their frozen order under EVAL-G02-028. Prior passing cells are immutable history, not evidence for
the new candidate.

## Hard boundary

The attempt may not modify `src/`, `deploy/`, `tests/`, `config/`, or `schemas/`, and may not add a
runner, fixture, framework, diagnostic tool, threshold, or operator pass route. Deterministic
failure terminates the attempt and creates a forward corrective; classified transient failures
resume only the affected atomic unit.

## Result

Four preflights, the real lab deployment, and all 60 access cells passed exactly once. The first
trace phase call then reached the operator relay without a current-candidate trace-service binding
and failed before its phase/trial journal could be written. Frozen diagnostics proved the service
process healthy and the candidate prerequisite absent from the passed lab dependency. The phase
was not retried; all downstream, destructive, Bailian, token, and cost work remained zero.
EVAL-G02-028 contains complete negative evidence with `open_evidence: []`; correction proceeds only
through C-G02-004.
