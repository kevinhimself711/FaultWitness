# A-G02-003 — Post-Egress Unified Candidate Gate Attempt

## Status

Active. This Gate attempt is separate from C-G02-003 and from every terminal attempt.

## Objective

After C-G02-003 closes, freeze a new candidate and execute the existing fourteen Gate phases in
their frozen order under EVAL-G02-028. Prior passing cells are immutable history, not evidence for
the new candidate.

## Hard boundary

The attempt may not modify `src/`, `deploy/`, `tests/`, `config/`, or `schemas/`, and may not add a
runner, fixture, framework, diagnostic tool, threshold, or operator pass route. Deterministic
failure terminates the attempt and creates a forward corrective; classified transient failures
resume only the affected atomic unit.
