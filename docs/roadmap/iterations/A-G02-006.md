# A-G02-006 — Prior-Recovery Unified Candidate Gate Attempt

## Status

Active on candidate `7fdb34999bef3413ccf16774440135730b23b5e4`. C-G02-006 is complete;
no completed work item is reopened.

## Objective

Orchestrate the frozen fourteen-phase G02 Gate Eval under EVAL-G02-034. Explicitly inherit only
semantically unchanged evidence: the 60-cell isolation, six-stage trace, 22-surface canary matrices,
and passed scenario seeds 1–3. Execute scenario seeds 4–32 and every affected downstream baseline,
reconciliation, and close-readiness phase.

## Hard boundary

This attempt may not modify `src/`, `deploy/`, `tests/`, `config/`, or `schemas/`, and may not add a
runner, fixture, framework, diagnostic tool, threshold, or operator pass route. A deterministic
failure terminates the attempt; a classified transient failure resumes only the affected trial.
