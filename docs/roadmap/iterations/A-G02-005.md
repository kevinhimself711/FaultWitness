# A-G02-005 — Post-Payment-Observer Unified Candidate Gate Attempt

## Status

Active after completed C-G02-005. This Gate attempt remains separate from every terminal attempt.

## Objective

After C-G02-005 closes, freeze a new candidate and orchestrate the existing fourteen Gate phases
in their frozen order under EVAL-G02-032. Prior attempt evidence remains immutable history.

## Hard boundary

The attempt may not modify `src/`, `deploy/`, `tests/`, `config/`, or `schemas/`, and may not add a
runner, fixture, framework, diagnostic tool, threshold, or operator pass route. Deterministic
failure terminates the attempt; classified transient failures resume only the affected atomic unit.
