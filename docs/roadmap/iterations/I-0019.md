# I-0019 — Baselines and Scoring

## Status

Corrective pass in progress. The original implementation candidate
`c4fa561ad58c5d8da0127fae8f0d0720eec15f76` and EVAL-G02-004 evidence remain preserved. A
pre-I-0020 audit found that the Gate baseline handlers only validated repository-external documents
instead of executing the frozen N=32/N=192 matrices. The owning Iteration is reopened before Gate
activation. Expected duration including the original Iteration Eval: **4h30**.

## Objective

Implement the deterministic, Naive ReAct, and no-RAG baselines over shared packets, deterministic
scoring, exact quality-floor registry, clustered bootstrap CI, and attributable usage/cost reporting.

The live adapters use the exact Bailian model with no fallback and no preset orchestration timeout.
This changes only orchestration liveness: sample counts, one internal transient retry, token/cost
ceilings, quality floors, latency reporting, oracle windows, and Gate criteria remain frozen.

## Falsifiable hypothesis

Four non-seed live trials cover both baselines and demonstrate that an interrupted trial resumes
without discarding the other completed results.

## Validation ownership

- L1: V-G02-012, V-G02-013.
- L3 Iteration evidence: V-G02-014 with N=3 and V-G02-015 with N=4.
- No L2 item is left for this Iteration; its large sample belongs to the L3 Gate layer.

The Iteration closed with `open_evidence: []`. Gate-layer N=32/N=192 work remains correctly owned by
I-0020 and is not Iteration evidence debt.

Implementation candidate: `c4fa561ad58c5d8da0127fae8f0d0720eec15f76`.

Evidence commit: `762164d74063a34be76a171ec53357c122dd1fdb`.

Runbook: `docs/runbooks/G02_BASELINES.md`.
