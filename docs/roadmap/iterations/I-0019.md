# I-0019 — Baselines and Scoring

## Status

Corrective pass completed on implementation candidate
`4bcc990c6b51d39736c81768199844b95f1a8c69` with EVAL-G02-004 passing and
`open_evidence: []`. The original candidate and evidence remain preserved. The corrective pass
replaced imported Gate baseline JSON with direct frozen N=32/N=192 execution interfaces without
running those Gate samples. Expected duration including Iteration Eval: **4h30**.

## Objective

Implement the deterministic, Naive ReAct, and no-RAG baselines over shared packets, deterministic
scoring, exact quality-floor registry, clustered bootstrap CI, and attributable usage/cost reporting.

The live adapters use the exact Bailian model with no fallback and no preset orchestration timeout.
This changes only orchestration liveness: sample counts, one internal transient retry, token/cost
ceilings, quality floors, latency reporting, oracle windows, and Gate criteria remain frozen.

## Falsifiable hypothesis

Four non-seed live trials cover both baselines and demonstrate that an interrupted trial resumes
without discarding the other completed results.

## Validation ownership

- L1: V-G02-012, V-G02-013.
- L3 Iteration evidence: V-G02-014 with N=3 and V-G02-015 with N=4.
- The direct Gate N=32/N=192 phase interfaces and negative paths are implemented and unit-tested;
  the large sample belongs to the L3 Gate layer and is only orchestrated by I-0020.

The Iteration closed with `open_evidence: []`. I-0019 owns the Gate runner implementation; I-0020
only executes it on the unified candidate. Gate N=32/N=192 execution is not Iteration evidence debt.

Implementation candidate: `4bcc990c6b51d39736c81768199844b95f1a8c69`.

Evidence commit: `5369c095b0f2e285227de5b958fd710b04e48360`.

Runbook: `docs/runbooks/G02_BASELINES.md`.
