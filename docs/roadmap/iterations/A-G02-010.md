# A-G02-010 — Clean-Lab Unified Candidate Gate Attempt

## Status

Terminally failed on candidate `9f51ddaf8c6d2168ef6676ccb8e8b133599cd2a9`. C-G02-010 remains
complete; no completed work item is reopened.

## Objective

Orchestrate the frozen fourteen-phase G02 Gate Eval under EVAL-G02-042. Inherit only deterministic
manifest debt and immutable G01 debt with `execution_count: 0`. Recreate the dedicated lab and
execute the 60-cell isolation, six-stage trace, 22-surface canary, all 32 scenarios, deterministic
baseline, 192 live trials, aggregate, reconciliation, and close-readiness on one candidate.

## Hard boundary

No remote matrix or scenario evidence from the deleted namespace is inherited. This attempt may
not modify product behavior, fixtures, test semantics, thresholds, or samples. A deterministic
failure terminates the attempt; a classified transport failure resumes only its affected unit.

## Result

All deterministic preflights passed. The candidate recreated a clean 24/24 Ready `fw-sut` and the
60-cell isolation, six-stage trace, and 22-surface canary matrices passed by real execution. Scenario
seed 1 (`kafkaQueueProblems`) passed with exact restore and healthy recovery. Seed 2
(`paymentUnreachable`) then completed its frozen 90-second observation window without a correlated
checkout/payment connection-error trace and was correctly recorded as `metric_fail`.

Post-failure readback proved both payment flags restored to `off`, all Deployments Ready, and the
frontend journey healthy. Source and historical-result comparison isolated one root cause: after
C-G02-010 reduced unsafe ambient load from ten users to one, the payment fault observers still relied
on incidental load-generator checkout timing. Unlike the Kafka branch, they had no candidate-bound
deterministic checkout stimulus. No baseline or model phase ran.
