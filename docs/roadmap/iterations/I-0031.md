# I-0031 — Fifth Replacement Unified Candidate Orchestration

## 状态

Terminal `failed` on candidate `0f3e83b871c8c69cfdb2c2321cc96e4894975d56`。除 Gate Eval 外
正常预计 **0h30**；Gate Eval 仍为 **2h33**，均不是 kill timeout。

## 目标

在 I-0030 关闭后冻结新候选，通过 EVAL-G02-016 顺序执行十四个既有 phase，完成
reconciliation 与 close-readiness，不在 orchestration 现场新增实现。

## 可证伪假设

新候选只编排已冻结 runner，即可通过全部 Gate 标准且无 waiver、fallback、blocked、
`infra_failed` 或 open evidence。

## 硬边界

确定性 implementation/policy/metric/quality/performance/reconciliation/close-readiness 失败会使
I-0031 永久 `failed`，并继续创建更高编号的前向 corrective/replacement Iteration。仅已
分类的瞬时基础设施或传输故障可在同一 phase/trial 续跑。

## 结果

四个 fail-fast preflight 与 `lab-deploy-and-bind` 通过。`isolation-access-matrix` 在
candidate-bound provisioning 时返回 `mc_admin_pod_deterministic_wait`；只读诊断证明固定
`minio/mc` digest 未进入 K3s，而节点 Docker Hub manifest request 发生 I/O timeout，Pod
进入 `ImagePullBackOff`。

provisioning、60 个 access cells、trace/canary/scenario、external service 与 model execution
均为 0。EVAL-G02-016 以 `open_evidence: []` 固化负证据；I-0032 前向修复 probe-image
offline staging，I-0033 承担替代编排。I-0017 与 I-0031 永不重开。
