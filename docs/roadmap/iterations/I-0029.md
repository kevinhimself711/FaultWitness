# I-0029 — Fourth Replacement Unified Candidate Orchestration

## 状态

Terminal `failed` on candidate `cfafa5a6f1510a1a7c8fe2c9360284214c8734f2`。除 Gate Eval 外
正常预计 **0h30**；Gate Eval 仍为 **2h33**，均不是 kill timeout。

## 目标

在 I-0028 关闭后冻结新候选，通过 EVAL-G02-014 顺序执行十四个既有 phase，完成
reconciliation 与 close-readiness，不在 orchestration 现场新增实现。

## 可证伪假设

新候选只编排已冻结 runner，即可通过全部 Gate 标准且无 waiver、fallback、blocked、
`infra_failed` 或 open evidence。

## 硬边界

确定性 implementation/policy/metric/quality/performance/reconciliation/close-readiness 失败会
使 I-0029 永久 `failed`，并继续创建更高编号的前向 corrective/replacement Iteration。仅已
分类的瞬时基础设施或传输故障可在同一 phase/trial 续跑。

## 结果

四个 fail-fast preflight 与 `lab-deploy-and-bind` 通过。`isolation-access-matrix` 在
candidate-bound provisioning 时先记录 `remote_probe_transport`；两次有界诊断确认远端
Python 3.8 无法导入 `datetime.UTC`，因此最终归类为确定性 runner compatibility failure。

provisioning、60 个 access cells、trace/canary/scenario/external-service/model execution 均为
0。EVAL-G02-014 以 `open_evidence: []` 固化负证据；I-0030 前向修复 Python 3.8 compatibility，
I-0031 承担替代编排。I-0024 与 I-0029 永不重开。
