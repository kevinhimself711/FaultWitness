# I-0024 — Candidate-Bound L2 Collector and Provisioning Corrective

## 状态

Completed。实现候选 `a2797037d1a3aa5c7f7264c2c3b122712b6d1677`；EVAL-G02-009
evidence commit `7e04d446bcd66fb4ed6bacef3c77503d8d043db8`。正常预计耗时
**3h00** 只用于可观测性与预算核对，不是 kill timeout。

## 目标

补齐 EVAL-G02-008 证明缺失的三个完整 L2 执行路径：候选绑定的隔离面 provisioning、
60-cell access collector、六阶段 correlated span collector，以及 22-surface Secret/PII
canary injector/collector。完成后 Gate handler 必须直接产生其输入和逐项证据，不再依赖
操作员预制 JSON。

## 可证伪假设

五个确定性用例足以证明 provisioning plan 和三个 collector 在候选/环境绑定、完整枚举、
负例拒绝、原子落盘与 phase 接口上可执行；测试期间不接触 live 环境，不执行任何 L2
矩阵、破坏性场景或模型调用。

## 硬边界

不改变 V-G02-009/010/011 的 Iteration N 或 Gate N，不改变 Ground Truth、locked tests、
质量/性能阈值、model route、token/cost ceiling 或失败语义。I-0018 与 I-0023 保持终态，
其资产不回写。

## 冻结实现路径

- `config/g02/gate-probes.yaml` 固定两个 digest-pinned probe image、六个 schema target、四个
  observability target 与四个 probe identity。
- `deploy/g02/gate_probe.py` 只经既有 host-pinned privileged SSH 执行；凭据在 private host
  生成并保留，不传回 operator。
- `g02_collectors.py` 逐 unit 原子写入 candidate/Eval-scoped journal，`infra_failed` 可续跑，
  `metric_fail`/`blocked` 不可在同一 candidate 重试。
- I-0024 Eval 只使用 `MemoryProbeBackend`，不会部署、调用外部服务、执行 Gate L2、破坏性
  场景或模型调用。

所有正常耗时操作都没有 orchestration kill timeout；这不改变 4/0/4 的 Iteration N、
60/6/22 的 Gate N 或任何质量、性能及零容忍裁决。

EVAL-G02-009 五项确定性 case 全部通过，`open_evidence: []`。Gate L2、private-server、
external service、破坏性场景和模型调用次数均为零。
