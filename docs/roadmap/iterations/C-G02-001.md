# C-G02-001 — Exact GetObject Probe Corrective

## 状态

Completed on candidate `0baa43d7004fe4cab270356ba27df68e7dfe3f1d`。工程修复与定向
验证分账；Gate attempt 与治理同步均不计入本 corrective。任何耗时估算都不是 kill timeout。

## 单一根因

EVAL-G02-020 的第一个对象读取单元使用 `mc stat`，客户端先要求 `ListBucket`，因此没有
真正测试冻结 policy 已授权的 `GetObject`。用户、policy attachment 与 sentinel 均正确。

## 最小范围

- 将对象读取改为不列目录的精确 `GetObject`。
- 复用现有 pytest，覆盖 allowed read、denied read、unchanged write 三个变化分支。
- 用真实 `mc` 客户端做一次定向 seam proof；它不是 60-cell Gate N，也不计为 Gate L2。

## 成本边界

不新增 Eval harness，不运行完整 Gate，不部署、不调用模型，不更新 Master Plan、全局 Claims
或最终验证路径。真实 seam evidence 固定写入
`docs/evals/EVAL-G02-023/artifacts/real-seam-proof.json`；完整统一候选执行只属于 A-G02-001。
关闭时必须 `open_evidence: []`。

## 结果

现有 pytest 入口的 3/3 变化分支通过；冻结真实 `minio/mc` 的 allow/deny GetObject 对照
通过。Gate L2、部署、destructive、外部服务与模型调用均为 0，`open_evidence: []`。
