# I-0032 — Digest-Pinned Probe Image Offline Staging Corrective

## 状态

Active。正常预计 **1h15**，仅为可观测计划值，不是 kill timeout。

## 目标

纠正 EVAL-G02-016 暴露的固定 probe image 可用性缺陷。现有 G02 离线 staging inventory
必须包含 `config/g02/gate-probes.yaml` 中恰好两个 digest-pinned images，并复用已经验证的
crane OCI archive、传输、K3s containerd import 与 digest check 路径。

## 可证伪假设

三个本地 deterministic cases 能证明 SUT 与 probe inventory 合并后恰好包含两项 probe
images、支持 `docker.io` reference normalization、去重且保持原 digest；Gate bootstrap
随后不再依赖私有节点直接访问 Docker Hub。

## 冻结验证边界

- corrective proof 仅 3 个 local cases；V-G02-017 Iteration N 仍为 0。
- V-G02-009/V-G02-010/V-G02-011 Iteration N 保持 4/0/4。
- remote Gate L2、破坏性、external service 与 model call 均为 0。
- 完成时 `open_evidence: []`；I-0017 与 I-0031 永不重开。
