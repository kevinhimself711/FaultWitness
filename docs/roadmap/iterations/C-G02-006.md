# C-G02-006 — Prior-Recovery Precondition Corrective

## Status

Active after terminal A-G02-005. This corrective owns only the redundant adjacent-seed control
sample exposed by EVAL-G02-032; it does not reopen A-G02-005 or I-0017.

## Falsifiable hypothesis

EVAL-G02-032 proves SEED-G02-0003 recovered with two healthy observations before SEED-G02-0004.
The frozen Master Plan permits those observations to prove the next precondition, but the runner
discarded them and took a one-shot control sample. If the prior recovery is absent or unhealthy,
the new path must reject it rather than bypass health.

## Minimal scope

The first seed retains its explicit control observation. Each later seed validates and records the
prior passed trial's two recovery observations as its precondition. Existing N, 30-second spacing,
90-second window, recovery semantics, thresholds, permissions and model budget remain unchanged.

Verification reuses the existing lab tests and executes only SEED-G02-0004 once with the preserved
SEED-G02-0003 recovery evidence. No full Gate phase, access/trace/canary matrix, baseline or model
call is part of this corrective. Closure requires `open_evidence: []`.
