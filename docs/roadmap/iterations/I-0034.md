# I-0034 — Containerd Imported-Reference Alias Resolution Corrective

## 状态

Active。正常预计 **1h00**，仅为可观测计划值，不是 kill timeout。

## 目标

纠正 EVAL-G02-018 暴露的 containerd import reference alias 缺陷。source resolver 只允许
`docker.io` 与 `index.docker.io` 两个等价 host alias，且 repository 与 manifest digest 必须
同时精确匹配；匹配后 tag 并复核冻结 target reference。

## 可证伪假设

三个本地 deterministic cases 能证明 requested source 已存在、仅 alias source 存在但 digest
精确、以及 alias digest 错误三条路径分别 pass、pass、fail closed；不需要运行远端 Gate phase。

## 冻结验证边界

- corrective proof 仅 3 个 local cases；V-G02-017 Iteration N 仍为 0。
- V-G02-009/V-G02-010/V-G02-011 Iteration N 保持 4/0/4。
- remote Gate L2、部署、破坏性、external service 与 model call 均为 0。
- 完成时 `open_evidence: []`；I-0017、I-0032 与 I-0033 永不重开。
