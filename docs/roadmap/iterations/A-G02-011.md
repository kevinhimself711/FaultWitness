# A-G02-011 — Deterministic-Workload Unified Candidate Gate Attempt

## Status

Terminally failed on candidate `bd374683de25b824af8669a993c8c7b771c6001e`. C-G02-011 remains
complete; no terminal work item is reopened.

## Objective

Orchestrate the frozen fourteen-phase G02 Gate Eval under EVAL-G02-044. Consume the exact-candidate
clean deployment checkpoint produced by EVAL-G02-043 rather than deleting and recreating the same
namespace again. Execute fresh 60-cell isolation, six-stage trace, 22-surface canary, all 32
scenarios, deterministic baseline, 192 live trials, aggregate, reconciliation, and close-readiness
on the current candidate and namespace epoch.

## Hard boundary

No remote matrix or scenario trial from an earlier namespace epoch is inherited. The deployment
checkpoint is reused with `execution_count: 0` because candidate, image/config/environment digests,
namespace epoch, and 24/24 readiness are unchanged after the corrective seam. This attempt may not
modify behavior, fixtures, test semantics, thresholds, or samples. A deterministic failure
terminates the attempt; only a classified transport failure resumes its affected unit.

## Result

All four deterministic preflights passed. The exact-candidate SUT deployment checkpoint was reused,
the missing trace-service candidate binding was completed through the existing deployer, and the
fresh 60-cell isolation matrix passed. The six-stage trace collector then terminally blocked before
stage records were produced.

Sanitized trace-service logs identified `PayloadConflict: trace reference reused with different
payload`. The first collection invocation had persisted the candidate-deterministic trace reference
before its outer command failed. Replay generated the same ID but a new wall-clock `emitted_at` and
span timestamps, so the persisted payload digest differed. This is a deterministic runner
idempotency defect, not a missing stage or quality failure. Canary, scenarios, baselines, and model
phases did not run; the 60 passing access cells remain immutable evidence.
