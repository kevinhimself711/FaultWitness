# C-G02-008 — Kafka Deterministic Workload Corrective

## Status

Active. A-G02-007 remains terminally failed and is not reopened.

## Root cause

The Kafka fault and exact sleep log were real, but the runner depended on the background load
generator to place an order. In SEED-G02-0013 the exported lag became positive only after the
frozen 90-second oracle deadline.

## Minimal correction

After exact flag readback, send one candidate-bound synthetic checkout through the existing frontend
API, then run the unchanged two-observation Kafka oracle and exact recovery. Unit tests prove the
stimulus is candidate-bound and occurs once. Targeted evidence runs only SEED-G02-0013; it is not a
Gate matrix and makes no model call.

## Frozen semantics

N=2, 30-second spacing, 90-second deadline, lag threshold, exact fault-log correlation, recovery,
permissions, Ground Truth, locked tests, quality/performance thresholds, and token/cost ceilings are
unchanged.
