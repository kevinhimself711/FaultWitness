# C-G02-004 — Candidate-Bound Trace Service Deployment Corrective

## Status

Active. This corrective owns the single trace-service prerequisite gap exposed by
EVAL-G02-028; it does not reopen A-G02-003 or I-0024.

## Falsifiable first action

The existing `inspect-trace-service` command must fail for A-G02-003's candidate while the existing
sanitized diagnosis shows the service process healthy. If that contrast is not present, stop and
reattribute the root cause before changing the lab handler. That contrast has been observed and is
preserved in EVAL-G02-028.

## Minimal scope

The existing `lab-deploy-and-bind` handler will call the already implemented trace-service deploy
and inspect paths for the exact candidate before declaring the lab dependency passed. Targeted
tests cover current-candidate success, stale-binding rejection, and ordering before the SUT pass.
The existing deploy/inspect/sanitized-smoke commands provide one real seam proof and drain any
pending trace buffer without running the six-stage Gate matrix. The trace-service rollout uses its
normal terminal result and has no preset wall-clock kill.

No new harness, full Gate phase, access/canary matrix, destructive action, Bailian call, model call,
threshold, permission, N, oracle, Ground Truth, locked-test, quality, performance, token/cost, or
failure-semantic change is authorized.
