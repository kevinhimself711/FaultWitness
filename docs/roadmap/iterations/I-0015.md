# I-0015 — G01 Candidate Audit and Failure Matrix

## Status

Completed on immutable candidate `cea2e63948d86bac3a3ae5d3afa68b477f20e3c2`.
The final candidate passed all three public required checks, the operator-approved
15-minute readiness adjudication, 14/14 walkthroughs, 36/36 live model trials,
all recovery and failure matrices, and final zero-backlog reconciliation.

Earlier audit candidate `bc8c040a1529c2b60702ed6db7d9b0358fc06661` established the first deployed service, runtime, network, coexistence, and two clean readiness checkpoints while the complete failure matrix still remained open.

Audit-head `aad15e2` added numeric SSE/persistence reference matrices, strict upstream-Eval debt blocking, and machine enforcement of the exact nine-asset closure boundary. These changes are included in deployed candidate `16294bc`.

Candidate `16294bc` includes those audit changes and is now deployed across all private components. Its platform, schema, OIDC API, sanitized Trace, authenticated Model Gateway, runtime, network, and coexistence checkpoint passed. Public cross-platform and remaining deep failure/recovery evidence are still required.

Later immutable checkpoints passed embedded-etcd restore, project-only Helm
rollback/reinstall, fresh-target PostgreSQL restore, all 82 real PostgreSQL
transitions, duplicate/crash/fencing matrices, Redis consumer recovery,
uncertain-ACK LangSmith replay, and a production-store 10,000-event/100-reconnect
API matrix. PR #18 passed all three required public checks on an earlier SHA.
The expanded live Control API path matrix passed on `c297dc6`; candidate
`5638eb9` then passed the cold-JWKS Keycloak outage fail-closed path with HTTP
401 before state mutation and full service recovery.

## Objective

Audit one immutable G01 candidate across public CI and the private server, demonstrate all frozen failure semantics, and prove readiness without closing the Gate.

## Scope

- Windows/Ubuntu verification, image builds, SBOM, vulnerability/license/secret/publication audits, and private-server clean-clone rehearsal.
- Fourteen G01 walkthroughs covering secrets, coexistence, runtimes, state/outbox, crashes, identity/SSE, tracing, models, policy, and rollback.
- Machine rejection of incomplete Iterations, failed/pending Evals, open evidence, queues, trace buffers, migration drift, and waivers.

## Non-goals

No post-candidate behavior or threshold change, G01 closure, G02 implementation, or inflated platform/model claim.

## Test and Eval

EVAL-G01-009 runs the complete candidate audit against one full SHA and verifies that close-mode rejects any extra implementation path or incomplete evidence.

## Pass criteria

- Required public and private evidence resolves to the same full SHA.
- Fourteen of fourteen walkthroughs pass.
- I-0007–I-0015 are completed and EVAL-G01-001–009 pass with zero open evidence, waiver, pending work, DLQ/backlog, or trace buffer.
- The audit does not close G01; closure remains a separate exact nine-asset commit.

Planned commit: `test(g01): run platform failure matrix and candidate audit`.
