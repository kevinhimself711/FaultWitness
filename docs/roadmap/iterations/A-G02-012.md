# A-G02-012 — Replay-Stable Unified Candidate Gate Attempt

## Status

In progress on candidate `86a459c0bec1bcf3e583fd1280fbca2667479d2e`. No terminal work item is
reopened.

## Objective

Orchestrate the frozen fourteen-phase G02 Gate Eval under EVAL-G02-046. Reuse only evidence whose
phase-owned behavior, fixtures, permissions, runtime/config/data subjects, environment, and external
routes are unchanged. Execute fresh trace, canary, all 32 scenarios, both baselines, aggregate,
reconciliation, and close-readiness.

## Hard boundary

The A-G02-011 60/60 access result is inherited with `execution_count: 0`; its source artifact,
timestamps, producing revision, phase record digest, and private journal remain unchanged. C-G02-012
changed only trace/canary timestamp construction and did not change access policy, identity, schema,
object prefix, observability reachability, credential, namespace, route, fixture, or runner branch.

The clean `fw-sut` runtime image/config/environment checkpoint is also reused without namespace
recreation because the SUT subjects are unchanged. The frozen binding-only operation verifies the
old provenance and 24/24 readiness before replacing only the candidate label, then verifies the same
image-set digest, readiness, zero restarts, and healthy no-fault state. Deployment execution count is
zero and binding update count is one.

Trace and canary execute fresh. All 32 scenarios execute fresh because C-G02-011 changed the payment
workload path. A deterministic failure terminates this attempt; only a classified transient failure
resumes its affected unit. No product behavior, fixture, threshold, sample, or Eval framework may be
added while this attempt is active.
